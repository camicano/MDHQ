require 'spec_helper'

describe 'ValueValidator class' do
	describe 'existsInRow?' do
		# it takes in an array and a value and it checks if that value exists
		# returns true is the value exists and false if it doesnt
	end

	describe 'existsInSquare?' do
		# it takes in a 3 arrays and a value and it checks if that value exists
		# returns true is the value exists and false if it doesnt
	end

	describe 'existsInColumn?' do
		# it takes in an array and a value and it checks if that value exists
		# returns true is the value exists and false if it doesnt
	end

	describe 'checkValue?' do
		# it checks if existsInRow, existsInSquare, existsInColumn return true or false
		# if any/all return true then the value doesnt work
		# if all return false then its a possible number
	end
end